{% for host in hosts %}
{% set host = host | regex_replace('.safe.ivbar.com','') %}
{% set newhost = override_hosts.get(host, host) %}
define service{
    use     generic-service
    name    {{ newhost }}
}
{% endfor %}
